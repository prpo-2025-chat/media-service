spring:
  application:
    name: media-service
  data:
    mongodb:
      uri: ${MONGO_URI}
      database: chat-app

server:
  port: 8083

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui
